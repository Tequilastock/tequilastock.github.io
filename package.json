{
  "name": "customsoftware-site",
  "private": true,
  "devDependencies": {
    "@lhci/cli": "^0.13.0",
    "eslint": "^10.1.0",
    "live-server": "^1.2.2",
    "prettier": "^3.8.1",
    "rimraf": "^5.0.10"
  },
  "scripts": {
    "lint": "eslint assets/js",
    "format": "prettier --write '**/*.{js,html,css,json}'",
    "dev": "live-server --port=3000 --quiet"
  }
}
